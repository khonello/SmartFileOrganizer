"""PySide6 desktop UI.

Import-guarded so the rest of the package stays testable without Qt installed:
importing this subpackage does not import Qt until a window class is used.
See DESIGN_SPEC.md for the full visual specification ("Ledger" theme).
"""
